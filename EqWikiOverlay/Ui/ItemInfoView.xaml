<UserControl x:Class="EqWikiOverlay.Ui.ItemInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignWidth="340" d:DesignHeight="420">
    <UserControl.Resources>
        <!-- Palette -->
        <SolidColorBrush x:Key="PanelBg" Color="#F21A1D28" />
        <SolidColorBrush x:Key="PanelBorder" Color="#FF3D4152" />
        <SolidColorBrush x:Key="Gold" Color="#FFF4C542" />
        <SolidColorBrush x:Key="HeaderBlue" Color="#FF7FB2FF" />
        <SolidColorBrush x:Key="BodyText" Color="#FFE9EBF2" />
        <SolidColorBrush x:Key="Muted" Color="#FF6C7183" />
        <SolidColorBrush x:Key="Link" Color="#FF6EA8FF" />
        <SolidColorBrush x:Key="Divider" Color="#22FFFFFF" />

        <BooleanToVisibilityConverter x:Key="BoolVis" />
    </UserControl.Resources>

    <Border Background="{StaticResource PanelBg}" CornerRadius="10" Padding="0"
            BorderBrush="{StaticResource PanelBorder}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="16" ShadowDepth="2" Opacity="0.6" />
        </Border.Effect>

        <Grid Margin="16,12,16,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Title + close -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding Title}" TextWrapping="Wrap"
                           Foreground="{StaticResource Gold}" FontSize="17" FontWeight="SemiBold" />
                <Button Grid.Column="1" x:Name="CloseButton" Content="✕"
                        Width="22" Height="22" Margin="8,0,0,0" Padding="0"
                        VerticalAlignment="Top" Cursor="Hand" ToolTip="Close (or release the hotkey)"
                        Foreground="{StaticResource Muted}" BorderThickness="0" FontSize="12"
                        Click="CloseButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#22FFFFFF" />
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#55E05555" />
                                    <Setter Property="Foreground" Value="White" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <Rectangle Grid.Row="1" Height="1" Fill="{StaticResource Divider}" Margin="0,10,0,0" />

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" MaxHeight="460" Margin="0,10,0,0">
                <StackPanel>
                    <!-- Single message line (loading / error / no result) -->
                    <TextBlock Text="{Binding Message}" TextWrapping="Wrap"
                               Foreground="{StaticResource Muted}" FontSize="13" FontStyle="Italic"
                               Visibility="{Binding HasMessage, Converter={StaticResource BoolVis}}" />

                    <!-- Structured sections -->
                    <ItemsControl ItemsSource="{Binding Sections}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="0,0,0,12">
                                    <!-- Section header. Muted when the section is just boilerplate. -->
                                    <TextBlock Text="{Binding Label}" FontSize="11" FontWeight="Bold"
                                               Margin="0,0,0,3">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="{StaticResource HeaderBlue}" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsEmptyTemplate}" Value="True">
                                                        <Setter Property="Foreground" Value="{StaticResource Muted}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>

                                    <ItemsControl ItemsSource="{Binding Lines}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <!-- DataContext is a WikiLine (Text + Kind). -->
                                                <Grid Margin="0,1,0,1">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="*" />
                                                    </Grid.ColumnDefinitions>

                                                    <!-- Bullet marker, only for Mob/Plain lines. -->
                                                    <TextBlock Grid.Column="0" Text="•" FontSize="13"
                                                               Margin="6,0,6,0" Foreground="{StaticResource Muted}">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Kind}" Value="Mob">
                                                                        <Setter Property="Visibility" Value="Visible" />
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding Kind}" Value="Plain">
                                                                        <Setter Property="Visibility" Value="Visible" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>

                                                    <TextBlock Grid.Column="1" Text="{Binding Text}"
                                                               TextWrapping="Wrap" FontSize="13">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <!-- Default: Plain -->
                                                                <Setter Property="Foreground" Value="{StaticResource BodyText}" />
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Kind}" Value="Zone">
                                                                        <Setter Property="Foreground" Value="{StaticResource Gold}" />
                                                                        <Setter Property="FontWeight" Value="SemiBold" />
                                                                        <Setter Property="Margin" Value="0,3,0,1" />
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding Kind}" Value="Mob">
                                                                        <Setter Property="Foreground" Value="{StaticResource BodyText}" />
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding Kind}" Value="Boilerplate">
                                                                        <Setter Property="Foreground" Value="{StaticResource Muted}" />
                                                                        <Setter Property="FontSize" Value="11" />
                                                                        <Setter Property="FontStyle" Value="Italic" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>

            <!-- Footer: source + wiki link -->
            <Grid Grid.Row="3" Margin="0,8,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding Status}" VerticalAlignment="Center"
                           Foreground="{StaticResource Muted}" FontSize="10" />
                <Button Grid.Column="1" x:Name="WikiButton" Content="Open full wiki page ↗"
                        Padding="8,4" Cursor="Hand" FontSize="12"
                        Foreground="{StaticResource Link}" Background="#22000000"
                        BorderBrush="{StaticResource PanelBorder}" BorderThickness="1"
                        Visibility="{Binding HasPageUrl, Converter={StaticResource BoolVis}}"
                        Click="WikiButton_Click" />
            </Grid>
        </Grid>
    </Border>
</UserControl>
